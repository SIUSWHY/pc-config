export interface CategoryType {
  title: string;
  icon: string;
}

export interface ItemType {
  model: string;
  price: string;
  memoryType: string;
  shopBrand: string;
  href: string;
  socket: string;
}
